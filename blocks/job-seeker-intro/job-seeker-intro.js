/*eslint-disable*/
function decorate(block) {
    // Deconstruct the first two divs inside the generated by AEM block
    const [div1, div2] = block.children;
    div1.className = 'job-seeker-intro__picture';
    const innerDiv1 = div1.querySelector('div');
    if (innerDiv1)
        innerDiv1.className = 'job-seeker-intro__inner';
    div2.className = 'job-seeker-intro__content';
    const innerDiv2 = div2.querySelector('div');
    if (innerDiv2)
        innerDiv2.className = 'job-seeker-intro__content-inner';
}
export default decorate;
